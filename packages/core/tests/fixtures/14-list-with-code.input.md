1. 먼저 패키지를 설치합니다.

   ```bash
   npm install md-to-naver-blog
   ```

2. 코드에서 사용합니다.

   ```javascript
   const { convert } = require("md-to-naver-blog");
   const result = convert(markdown);
   ```

3. 결과를 확인합니다.
